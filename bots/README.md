# Bots

Python keeper bots for HedgeFi. Use the local virtual environment in this folder.

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python oracle_keeper.py
```
